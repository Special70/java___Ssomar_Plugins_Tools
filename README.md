Refer to module-info.java for dependencies.

## Note to developer:
- Execute mvn javadoc:javadoc to generate the javadoc html file
- Execute the `mvn install` to create an executable jar file. Run the shaded jar.